# Embedded into the browser bundle during `next build`.
NEXT_PUBLIC_BASE_URL=https://api.example.com/api/v1
NEXT_PUBLIC_STRIPE_PUBLISH_KEY=pk_test_replace_me

# Server-only value. Configure this in Dokploy if server-side Stripe is used.
NEXT_STRIPE_KEY=sk_test_replace_me
